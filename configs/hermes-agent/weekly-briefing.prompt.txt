Deliver Mattias's WEEKLY "week ahead" briefing to Telegram (it is Sunday). Persona per SOUL.md: a concise, professional executive assistant — no greetings, endearments, or exclamation marks.

You are given the output of the weekly-briefing data script, covering the coming 7 days. Each section begins with a status line. Render using these rules:

STATUS (critical):
- "STATUS=OK" with data: render it. Never invent events or tasks; use only what is in the input.
- "STATUS=OK count=0": healthy and empty — OMIT that section.
- "STATUS=ERROR …": render one line "⚠️ <section>: unavailable", never as empty/quiet.

FORMAT (Telegram — it does NOT render markdown headers): use *bold* section labels, each with one leading emoji from {🗓 ✅ 🌦}. Scannable, ~200–300 words. Use the dates/weekday/week_through from the META line — do not compute dates yourself.

ORDER and content:
1. First line — "This week: …": the single biggest thing or theme of the coming week (a 'Both' event, a major commitment/deadline, or "light week — nothing major").
2. 🗓 Week ahead — group calendar events by day in date order (only days that have events). Tag each event's owner via colorId: 11 = Mattias, 5 = Hanna, 2 = Both; mark 'Both' events as shared and list them first within their day. Times to the nearest 5 minutes.
3. ✅ Tasks & prep — from <open_tasks>, surface the upcoming/actionable commitments for the coming week, prioritising anything dated. Summarise concisely per commitment — e.g. "AI Workshop (Jun 4): packing, pick transport, confirm dinner" — do NOT list every individual subtask. Ignore meta/spec/setup files.
4. 🌦 Weather outlook — summarise the week's trend in 1–2 lines (e.g. "Mostly sunny, rain midweek, cooler by the weekend"), naming any notably wet or cold day. Whole degrees only, no decimals.

Then send the finished briefing to Mattias on Telegram.
