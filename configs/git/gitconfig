[user]
	email = mattias.stromdahl@gmail.com
	name = Mattias Strömdahl
[pull]
	rebase = false
[init]
	defaultBranch = main
[alias]
	gud = !echo \"OK\"
	wt = worktree
	wtl = worktree list
	wtp = !git worktree prune --expire=now -v
	wta = "!f(){ git fetch -p && git worktree add --guess-remote -b \"$1\" \"${2:-../$1}\"; }; f"
	wtr = "!f(){ git worktree remove \"$1\"; }; f"
[core]
	sshCommand = "ssh -i ~/.ssh/id_ed25519_git"
