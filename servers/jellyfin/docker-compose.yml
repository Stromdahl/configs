
networks:
  gluetun_network:
  socket_proxy:

volumes:
  qbittorrent_volume:
  jellyfin_volume:
  jellyseerr_volume:
  traefik_certs:

  # *arr
  bazarr_volume:
  profilarr_volume:
  prowlarr_volume:
  radarr_volume:
  sonarr_volume:

secrets:
  cf_dns_api_token:
    environment: CF_DNS_API_TOKEN

services:

  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:v0.4.2
    container_name: docker-socket-proxy
    restart: unless-stopped
    environment:
      - CONTAINERS=1
      - NETWORKS=1
      - SERVICES=0
      - TASKS=0
      - INFO=1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - socket_proxy

  traefik:
    image: traefik:v3.6.11
    container_name: traefik
    restart: unless-stopped
    depends_on:
      - docker-socket-proxy
    command:
      - "--api.dashboard=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.endpoint=tcp://docker-socket-proxy:2375"
      - "--providers.file.filename=/etc/traefik/dynamic.yml"
      - "--entrypoints.web.address=:80"
      - "--entryPoints.web.http.redirections.entryPoint.to=websecure"
      - "--entryPoints.web.http.redirections.entryPoint.scheme=https"
      - "--entryPoints.web.http.redirections.entryPoint.permanent=true"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.provider=cloudflare"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL:?missing ACME_EMAIL}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${DOMAIN:?missing DOMAIN}`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.middlewares=dashboard-auth,security-headers@file"
      - "traefik.http.middlewares.dashboard-auth.basicauth.users=${TRAEFIK_DASHBOARD_AUTH:?missing TRAEFIK_DASHBOARD_AUTH}"
    ports:
      - "80:80"
      - "443:443"
    secrets:
      - cf_dns_api_token
    environment:
      - CF_DNS_API_TOKEN_FILE=/run/secrets/cf_dns_api_token
    volumes:
      - traefik_certs:/letsencrypt
      - ./config/traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro
    networks:
      - gluetun_network
      - socket_proxy

  jellyfin:
    image: ghcr.io/linuxserver/jellyfin:10.11.8
    container_name: jellyfin
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jellyfin.rule=Host(`${DOMAIN:?missing DOMAIN}`)"
      - "traefik.http.routers.jellyfin.entrypoints=websecure"
      - "traefik.http.routers.jellyfin.tls.certresolver=letsencrypt"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
      - "traefik.http.routers.jellyfin.middlewares=jellyfin-headers@file"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:8096/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      # - JELLYFIN_PublishedServerUrl=${JELLYFIN_URL}
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    devices:
     - /dev/dri:/dev/dri #Required for jellyfin HW transcoding / QuickSync
    volumes:
      - jellyfin_volume:/config
      - ${MEDIA_SHARE}:/media
    networks:
      - gluetun_network

  gluetun:
    image: qmcgaw/gluetun:v3.41.1
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      # qbittorrent subdomain
      - "traefik.http.routers.qbittorrent.rule=Host(`qbittorrent.${DOMAIN:?missing DOMAIN}`)"
      - "traefik.http.routers.qbittorrent.entrypoints=websecure"
      - "traefik.http.routers.qbittorrent.tls.certresolver=letsencrypt"
      - "traefik.http.routers.qbittorrent.service=qbittorrent"
      - "traefik.http.services.qbittorrent.loadbalancer.server.port=8080"
      - "traefik.http.routers.qbittorrent.middlewares=security-headers@file"
      # prowlarr at /prowlarr
      - "traefik.http.routers.prowlarr.rule=Host(`${DOMAIN:?missing DOMAIN}`) && PathPrefix(`/prowlarr`)"
      - "traefik.http.routers.prowlarr.entrypoints=websecure"
      - "traefik.http.routers.prowlarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.prowlarr.service=prowlarr"
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"
      - "traefik.http.routers.prowlarr.middlewares=security-headers@file"
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- https://am.i.mullvad.net/connected 2>/dev/null | grep -q 'You are connected'"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s
    environment:
      - VPN_SERVICE_PROVIDER=${GLUETUN_VPN_SERVICE_PROVIDER:?missing GLUETUN_VPN_SERVICE_PROVIDER}
      - VPN_TYPE=wireguard
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY:?Missing wireguard private key}
      - WIREGUARD_ADDRESSES=${WIREGUARD_ADDRESS:?missing WIREGUARD_ADDRESS}
      - SERVER_CITIES=${GLUETUN_SERVER_CITIES:?missing GLUETUN_SERVER_CITIES}
    ports:
      - 8080:8080 # Qbittorent
      - 6881:6881 # Torrent
      - 6881:6881/udp # Torrent
      - 8191:8191/tcp # flaresolverr
      - 9696:9696/tcp # prowlarr
    networks:
      gluetun_network:

  #Flaresolerr - Used as a proxy server to bypass Cloudflare and DDoS-GUARD protection.
  flaresolverr:
    image: flaresolverr/flaresolverr:v3.4.6
    container_name: flaresolverr
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8191/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    labels:
      - com.centurylinklabs.watchtower.enable=false
    environment:
      - LOG_LEVEL=info
      - LOG_HTML=false
      - CAPTCHA_SOLVER=none
      - TZ=${TZ:-Europe/Stockholm}
      - LANG=en_GB
      - DRIVER=nodriver
    restart: unless-stopped

  test:
    image: alpine:3.22
    command: sh -c "apk add wget && wget -qO- https://ipinfo.io"
    network_mode: "service:gluetun"
    labels:
      - com.centurylinklabs.watchtower.enable=false

  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:5.1.4
    container_name: qbittorrent
    restart: unless-stopped
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
      - WEBUI_PORT=8080
    volumes:
      - qbittorrent_volume:/config
      - ${MEDIA_SHARE?}:/data/media
      - ${DOWNLOADS_SHARE?}:/downloads

  #Radarr - used to find movies automatically
  radarr:
    image: lscr.io/linuxserver/radarr:6.1.1
    container_name: radarr
    restart: unless-stopped
    depends_on:
      qbittorrent:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.radarr.rule=Host(`${DOMAIN:?missing DOMAIN}`) && PathPrefix(`/radarr`)"
      - "traefik.http.routers.radarr.entrypoints=websecure"
      - "traefik.http.routers.radarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.radarr.loadbalancer.server.port=7878"
      - "traefik.http.routers.radarr.middlewares=security-headers@file"
      - "backup-stop=true"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7878/ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    volumes:
      - radarr_volume:/config
      - ${MEDIA_SHARE?}:/data/media
      - ${DOWNLOADS_SHARE?}:/downloads
    networks:
      gluetun_network:

  #Sonarr - used to find series automatically
  sonarr:
    image: lscr.io/linuxserver/sonarr:4.0.17
    container_name: sonarr
    depends_on:
      qbittorrent:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sonarr.rule=Host(`${DOMAIN:?missing DOMAIN}`) && PathPrefix(`/sonarr`)"
      - "traefik.http.routers.sonarr.entrypoints=websecure"
      - "traefik.http.routers.sonarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"
      - "traefik.http.routers.sonarr.middlewares=security-headers@file"
      - "backup-stop=true"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8989/ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    volumes:
      - sonarr_volume:/config
      - ${MEDIA_SHARE?}:/data/media
      - ${DOWNLOADS_SHARE?}:/downloads
    networks:
      gluetun_network:
    restart: unless-stopped

  #Bazarr - used to automatically download subtitles for movies and series
  bazarr:
    image: lscr.io/linuxserver/bazarr:1.5.6
    container_name: bazarr
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bazarr.rule=Host(`${DOMAIN:?missing DOMAIN}`) && PathPrefix(`/bazarr`)"
      - "traefik.http.routers.bazarr.entrypoints=websecure"
      - "traefik.http.routers.bazarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.bazarr.loadbalancer.server.port=6767"
      - "traefik.http.routers.bazarr.middlewares=security-headers@file"
      - "backup-stop=true"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:6767/bazarr/ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    volumes:
      - bazarr_volume:/config
      - ${MEDIA_SHARE?}:/data/media
    networks:
      gluetun_network:

  profilarr:
    image: santiagosayshey/profilarr:v1.1.4
    container_name: profilarr
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.profilarr.rule=Host(`${DOMAIN:?missing DOMAIN}`) && PathPrefix(`/profilarr`)"
      - "traefik.http.routers.profilarr.entrypoints=websecure"
      - "traefik.http.routers.profilarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.profilarr.loadbalancer.server.port=6868"
      - "traefik.http.routers.profilarr.middlewares=security-headers@file"
      - "backup-stop=true"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:6868/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - profilarr_volume:/config
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    networks:
      gluetun_network:

  jellyseerr:
    image: fallenbagel/jellyseerr:2.7.3
    container_name: jellyseerr
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jellyseerr.rule=Host(`jellyseerr.${DOMAIN:?missing DOMAIN}`)"
      - "traefik.http.routers.jellyseerr.entrypoints=websecure"
      - "traefik.http.routers.jellyseerr.tls.certresolver=letsencrypt"
      - "traefik.http.services.jellyseerr.loadbalancer.server.port=5055"
      - "traefik.http.routers.jellyseerr.middlewares=security-headers@file"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5055/api/v1/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    environment:
      - TZ=${TZ:-Europe/Stockholm}
    volumes:
      - jellyseerr_volume:/app/config
    networks:
      - gluetun_network

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:2.3.5
    container_name: prowlarr
    network_mode: "service:gluetun"
    labels:
      - "backup-stop=true"
    depends_on:
      gluetun:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9696/ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      - PUID=${UID:?missing UID}
      - PGID=${GID:?missing GID}
      - TZ=${TZ:-Europe/Stockholm}
    volumes:
      - prowlarr_volume:/config
    restart: unless-stopped

#   backup:
#     image: offen/docker-volume-backup:latest
#     container_name: backup
#     restart: unless-stopped
#     environment:
#       - BACKUP_CRON_EXPRESSION=0 2 * * *
#       - BACKUP_FILENAME=backup-%Y-%m-%dT%H-%M-%S.tar.gz
#       - BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-7}
#       - BACKUP_STOP_DURING_BACKUP_LABEL=backup-stop
#     volumes:
#       - /var/run/docker.sock:/var/run/docker.sock:ro
#       - ${BACKUP_DIR:?missing BACKUP_DIR}:/archive
#       - jellyfin_volume:/backup/jellyfin:ro
#       - jellyseerr_volume:/backup/jellyseerr:ro
#       - qbittorrent_volume:/backup/qbittorrent:ro
#       - radarr_volume:/backup/radarr:ro
#       - sonarr_volume:/backup/sonarr:ro
#       - bazarr_volume:/backup/bazarr:ro
#       - prowlarr_volume:/backup/prowlarr:ro
#       - profilarr_volume:/backup/profilarr:ro
#       - traefik_certs:/backup/traefik_certs:ro
#
  homer:
    image: b4bz/homer:v24.12.1
    container_name: homer
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homer.rule=Host(`homer.${DOMAIN:?missing DOMAIN}`)"
      - "traefik.http.routers.homer.entrypoints=websecure"
      - "traefik.http.routers.homer.tls.certresolver=letsencrypt"
      - "traefik.http.services.homer.loadbalancer.server.port=8080"
      - "traefik.http.routers.homer.middlewares=security-headers@file"
    user: "${UID:?missing UID}:${GID:?missing GID}"
    volumes:
      - ./config/homer/config.yml:/www/assets/config.yml:ro
    networks:
      - gluetun_network
