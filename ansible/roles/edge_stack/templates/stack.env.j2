# Rendered by Ansible from roles/edge_stack/templates/stack.env.j2.
# Mode 0600 on the host — do NOT commit the rendered file on radon.
# No secrets live here (the origin key is rendered to disk by secrets.yml, not
# via this env). Compose ${VAR} refs in docker-compose.yml resolve from here.

SETTLEUP_IMAGE={{ settleup_image }}
SETTLEUP_HOST={{ settleup_host }}
SETTLEUP_BASE_URL={{ settleup_base_url }}
