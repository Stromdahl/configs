# Fallback profile used when hosts/<hostname>/modules.conf doesn't exist.
# Keep this conservative: only modules that are safe on any Debian/Ubuntu box.
# After first bootstrap, create hosts/<your-hostname>/modules.conf tailored
# to that machine (laptop vs desktop, sway vs headless, etc.).

base
ssh
bash
git
gtk
userdirs
fzf
nvim
network-tools
unattended-upgrades
