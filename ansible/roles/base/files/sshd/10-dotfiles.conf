# Managed by Ansible (roles/base). Key-only auth, no root login.
# Byte-identical to the dotfiles modules/sshd drop-in so helium matches the fleet.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
