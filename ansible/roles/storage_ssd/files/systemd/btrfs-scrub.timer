[Unit]
Description=Monthly btrfs scrub of the helium-ssd pool

[Timer]
# 1st of the month, 05:00 — off the SnapRAID sync (nightly 03:00) and scrub
# (Sun 04:00) windows so the two storage tiers never scrub at once.
OnCalendar=*-*-01 05:00:00
Persistent=true

[Install]
WantedBy=timers.target
