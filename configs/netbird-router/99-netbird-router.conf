# Sysctl drop-in for a host acting as a NetBird routing peer.
# Enables IPv4/IPv6 forwarding so the daemon can route packets between
# the WireGuard interface (wt0) and the upstream LAN.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
