# Modules to install on 'krypton' (laptop). One per line.
# See modules/<name>/install.sh for what each does.

base
ssh
bash
git
gtk
userdirs
xkb
fzf

# Editor
nvim

# Languages
node
rust
python

# Tools
wl-clipboard
tmux
docker
netbird
signal

# Claude Code personal skills (~/.claude/skills -> configs/claude/skills)
claude

# Wayland/sway desktop
sway
waybar
foot
kanshi
mako
swaync

# Utilities
yazi
syncthing
battery-guardian
network-tools
unattended-upgrades

# Daily local bup backup of ~/notes (~/backups/bup-notes)
bup

# msbrain autonomous agent — interim host (ADR 0005). Installs systemd --user units
# DISABLED; arm them manually after provisioning the personal key (see module header).
msbrain
